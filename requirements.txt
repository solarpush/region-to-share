# Core GUI Framework
PyQt5>=5.15.0
PyQt5-stubs>=5.15.6.0

# Screen Capture Libraries
mss>=10.0.0
opencv-python-headless>=4.12.0
numpy>=2.0.0
pyscreenshot>=3.1

# Image Processing
Pillow>=11.0.0

# D-Bus Communication (Essential for GNOME Mutter ScreenCast)
dbus-python>=1.3.0

# System Integration
python-xlib>=0.33

# Alternative D-Bus implementation (fallback)
jeepney>=0.8.0

# Utilities
six>=1.17.0
EasyProcess>=1.1
entrypoint2>=1.1

# Cairo bindings (for advanced graphics if needed)
pycairo>=1.28.0

# Note: PyGObject/GI is required for GStreamer but typically installed system-wide
# on GNOME systems. For development, install via system package manager:
# Ubuntu/Debian: sudo apt install python3-gi python3-gi-cairo gir1.2-gstreamer-1.0
# Fedora: sudo dnf install python3-gobject python3-gobject-devel gstreamer1-python
# Arch: sudo pacman -S python-gobject gstreamer python-gst
